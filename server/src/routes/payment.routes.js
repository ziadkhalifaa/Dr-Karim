import express, { json } from "express";
import { requireAuth, requireRole } from "../middleware/auth.js";
import { paymentController } from "../controllers/payment.controller.js";
// Receipts arrive as base64 data URLs (≤5 MB decoded → ~7 MB encoded).
const RECEIPT_JSON = json({ limit: "8mb" });
export function paymentRouter() { const r = express.Router(); r.get("/packages", paymentController.packages); r.get("/packages/:id", paymentController.package); r.get("/payment-settings", paymentController.settings); r.use(requireAuth); r.post("/payments", requireRole("patient"), paymentController.create); r.get("/patient/payments", requireRole("patient"), paymentController.list); r.get("/patient/payments/:id", requireRole("patient"), paymentController.get); r.post("/payments/:id/receipt", requireRole("patient"), RECEIPT_JSON, paymentController.receipt); r.get("/payments/:id/receipt", requireRole("patient", "doctor", "staff"), paymentController.receiptFile); r.get("/doctor/payments", requireRole("doctor", "staff"), paymentController.doctorList); r.get("/doctor/payments/:id", requireRole("doctor", "staff"), paymentController.doctorGet); r.post("/doctor/payments/:id/approve", requireRole("doctor", "staff"), paymentController.approve); r.post("/doctor/payments/:id/reject", requireRole("doctor", "staff"), paymentController.reject); r.get("/patient/entitlements", requireRole("patient"), paymentController.entitlements); return r; }
